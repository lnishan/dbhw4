sudo: false
language: cpp

compiler:
  - gcc
  - clang

install:
    - alias g++=g++-4.8
    - make

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
    - clang
